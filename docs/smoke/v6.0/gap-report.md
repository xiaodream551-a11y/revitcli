# RevitCli v6.0 Local BIMOps Workbench Gap Report

This v6.0 slice is a contract baseline for the Local BIMOps Workbench and the
Revit Model Operations Ledger. It now includes read-only standards validate,
issue preflight, dry-run issue package, read-only deliverables verify,
read-only ledger query, read-only ledger validate, read-only ledger stats,
read-only ledger timeline, local ledger analytics snapshot bundling, append-only local ledger records, ledger replay preview, and read-only
workflow registry commands over local artifacts. The portable command spine also executes signed `journal verify`,
`history list` JSON/table outputs, and `rollback --dry-run` against synthetic
local fixtures. Supported command-spine paths include table summary and
Markdown detail parity checks derived from JSON payloads. It does not implement
a ledger database runtime, Revit write behavior beyond approved `set` writes,
bounded set replay apply, bounded receipt-backed export replay apply, and
bounded manifest-backed schedule batch-export replay apply, SaaS, MCP,
built-in LLM behavior, or
dashboard-central state.

| Scope | Status | Evidence |
| --- | --- | --- |
| Contract baseline | portable verified | `docs/v6-local-bimops-contract.md` defines command spine, ledger fields, dry-run rules, deterministic receipt requirements, rollback preconditions, and audit trail invariants. |
| Read-only standards validate | portable verified | `standards validate --output json` reads local standards manifests and project files, reports standards runtime issues, preserves table summary and Markdown detail parity for key validation fields, includes populated-target dry-run final file-tree snapshot evidence for standards install planning, and does not start Revit or write model data. |
| Issue preflight and dry-run package | portable verified | `issue preflight --output json` and `issue package --dry-run --output json` read local issue profiles, enforce hidden mutation guards, preserve table summary and Markdown detail parity for checks, package files, planned actions, and command paths, and produce dry-run package traceability without writing bundles or receipts. |
| Read-only deliverables verify | portable verified | `deliverables verify --output json` reads local delivery manifests and readable-receipt evidence, reports missing receipts, preserves Kinds and Outcomes counts in table and Markdown output, and runs without package writes. |
| Local audit spine | portable verified | `journal verify --output json`, `history list` JSON/table outputs, and `rollback --dry-run` execute through the public CLI parser against signed journal, history-list.v1, and safe rollback-preview fixtures with journal verify JSON/table validity/root-hash parity, history-list.v1 JSON count consistency and table row-order parity, rollback dry-run request enforcement, final file-tree snapshots, and event-level no-write evidence. |
| Append-only ledger runtime | portable verified | `ledger append --output json` emits `ledger-append.v1` as a dry-run by default, writes `.revitcli/ledger/operations.jsonl` only with `--yes`, rejects invalid status/timestamp/receipt hash input, records affected count/id evidence when supplied, and makes appended `ledger-operation.v1` records visible through the `ledger` source. |
| Ledger replay | portable verified | `ledger replay --source ledger --output json` emits `ledger-replay.v1` from appended ledger records as a default preview with `dryRun=true`, per-step `canApply`/`blockReason`, JSON/table/Markdown output semantic parity, final file-tree snapshot evidence, and event-level no-write evidence without writing files; `ledger replay --source ledger --action set --apply --yes --output json` is bounded to successful approved `set --yes` source-ledger records with frozen affected ids; `ledger replay --source ledger --action export --apply --yes --output json` is bounded to successful receipt-backed export source-ledger records with recorded format/selector/output-dir args; `ledger replay --source ledger --action schedules.batch-export --apply --yes --output json` is bounded to successful manifest-backed schedule batch-export records with recorded CSV output entries; successful apply appends a non-replayable `ledger.replay.apply` audit record with best-effort live model identity/path/version. |
| Read-only ledger query | portable verified | `ledger query --output json` emits `ledger-query.v1` from journal, history, delivery manifest, workflow receipt files, and appended local ledger records with deterministic timestamp/source/path/line ordering, JSON/table/Markdown output semantic parity, malformed artifact issues, final file-tree snapshot evidence, and event-level no-write evidence. |
| Read-only ledger validate | portable verified | `ledger validate --output json` emits `ledger-validate.v1` and checks source readability, artifact links, receipt status, declared receipt hash values, timestamp format, validation JSON/table/Markdown semantic parity, final file-tree snapshot evidence, and event-level no-write evidence without writing files. |
| Read-only ledger stats | portable verified | `ledger stats --output json` emits `ledger-stats.v1` with source counts, action counts, category and operator counts, receipt status counts, malformed artifact issue source counts, issue severity counts, JSON/table/Markdown stats semantic parity, final file-tree snapshot evidence, and event-level no-write evidence without writing files by default; repeated `--project` roots add local cross-project `projectDirectories` and `byProject` counts without a database; explicit `--analytics-snapshot` persists a single local `ledger-stats.v1` JSON artifact and `--from-analytics-snapshot` reads it back. |
| Read-only ledger timeline | portable verified | `ledger timeline --output json` emits `ledger-timeline.v1` with bucket, source, action, category counts per bucket, operator counts per bucket, receipt status, issue severity, JSON/table/Markdown timeline semantic parity, unbucketed timestamp evidence, final file-tree snapshot evidence, and event-level no-write evidence without writing files by default; repeated `--project` roots add local cross-project `projectDirectories` and `byProject` counts without a database; explicit `--analytics-snapshot` persists a single local `ledger-timeline.v1` JSON artifact and `--from-analytics-snapshot` reads it back. |
| Local ledger analytics bundle | portable bounded | `ledger analytics --output-dir .revitcli/analytics --output json` emits `ledger-analytics-bundle.v1` and writes local `ledger-stats.v1` plus `ledger-timeline.v1` snapshots with explicit local-only/no-database/no-network-service boundary flags; this packages deterministic evidence but does not claim an analytics service runtime. |
| Read-only workflow registry | portable verified | `workflow registry --output json` emits `workflow-registry.v1` with inputs, outputs, read/write scope, risk level, dry-run command, approval command, rollback support, receipt schema, acceptance evidence, JSON/table/Markdown output semantic parity, final file-tree snapshot evidence, and event-level no-write evidence without writing files. |
| Workbench runtime evidence binding | portable verified | The scoped `v60LocalBimOpsContractGate` emits structured `runtimeEvidence`, including command-spine subfields for `standardsValidate`, `issuePreflight`, `issuePackageDryRun`, `deliverablesVerify`, `journalVerify`, `historyList`, `historyListCountConsistency`, `historyListRowOrder`, `historyListEvidence`, `rollbackDryRun`, `rollbackDryRunPreview`, `rollbackNoMutatingSetRequest`, `rollbackDryRunEvidence`, `commandSpineOutputParity`, and `commandSpineNoWrites`; `release verify --strict` consumes and re-emits those structured booleans and validates nested history-list count/order evidence plus rollback dry-run no-mutation evidence instead of trusting status text alone. |
| Revit 2026 live add-in smoke | live verified | `docs/smoke/v6.0/revit2026-live-addin.md` records `doctor --check-version 2026`, `status`, `query --id`, filtered query, set dry-run, approved set, confirm, restore, local ledger record evidence with model identity/path/version when status is available, bounded `ledger replay --source ledger --action set --apply --yes`, receipt-backed PDF `export` ledger evidence, bounded `ledger replay --source ledger --action export --apply --yes`, manifest-backed `schedules batch-export` ledger evidence, and bounded `ledger replay --source ledger --action schedules.batch-export --apply --yes` against the controlled `revit_cli.rvt` model with CLI/add-in/live add-in version alignment. The 2026-05-27 WSL helper refresh adds a no-write `scripts/smoke-revit-wsl.sh` path with machine-readable `summary.json` for open-Revit validation when PowerShell is unavailable in WSL, records source-vs-installed version drift instead of treating the installed Windows CLI as current-source proof, and exposes `--require-current-source` with machine-readable `currentSourceDriftKind` values. `install-required` produces reinstall/restart `nextActions` plus a generated `install-current-source.ps1` evidence handoff and repo-tracked `scripts\install-current-source-revit2026.ps1`; `restart-required` records staged add-in evidence through `stagedAddinCommit` and `stagedAddinPath`, emits restart/rerun `nextActions`, and does not generate an install handoff when the checked-out source is already staged but the open Revit process still has an older add-in loaded. The hot-reload/Core split refresh for commit `9633727e8c1df77fa50502cd017b82043e833e8a` passed the no-write current-source helper with `currentSourceDriftKind=none`, matching CLI/installed/live/status commits, and `mutatesModel=false` after a controlled Revit close, signed stable-loader install, and restart. The 2026-05-29 current-source helper refresh also passed with matching CLI/installed/live/status commits, `currentSourceInstalled=true`, `currentSourceDriftKind=none`, and `mutatesModel=false` after reinstall and restart. |
| Operations ledger | staged live-write runtime | The Revit Model Operations Ledger now has a local append-only JSONL writer, read path, default replay preview, approved `set` write ledger records after successful Revit API writes with best-effort live model identity/path/version, receipt-backed `export` ledger records after successful export receipt writes, `schedules batch-export` ledger rows for succeeded and manifest-recorded failed batches, live-verified bounded set replay apply, live-verified bounded export replay apply, bounded schedule batch-export replay apply, replay-apply audit rows with the same status-derived evidence when available, explicit local cross-project stats/timeline over supplied directories, local single-file stats/timeline analytics snapshots, and a local stats/timeline analytics bundle command; broader live ledger apply/replay runtime, analytics service runtime, and broader live Revit ledger integration are not claimed in this slice, and no database runtime is introduced. |
| Standards runtime | portable bounded | Standards remain local files validated by terminal commands; no cloud policy service is introduced. |
| Project memory | portable bounded | Project memory is defined as local history from receipts, journals, manifests, and reports, not LLM memory. |
| Workflow registry | portable bounded | Workflows remain governed local files with declared inputs, outputs, read/write scope, risk level, dry-run command, approval command, rollback support, receipt schema, and acceptance evidence. |
| Office rollout evidence pack | documented, not live verified | `docs/smoke/v6.0/pilot-evidence-template.md` is the v6.0 pilot evidence packet for controlled project-copy pilots, and `docs/smoke/v6.0/release-pilot-spine.md` records the synthetic CLI fixture for the release pilot spine without claiming real office rollout evidence. `release pilot scaffold` creates a public-safe packet scaffold under `docs/smoke/v6.0/` without mutating rollout status and reports scaffold `nextActions` for validate/register intake, `release pilot validate` checks the packet before it is listed as completed evidence and reports validate `nextActions`; unsafe paths route back to a public-safe scaffold path and missing packets route to scaffold with the requested safe path, `release pilot register` dry-runs or writes the completed-pilot status entry only after validation and reports `completedOfficePilotCountBefore`, `completedOfficePilotCountAfter`, and register nextActions for validation failures, duplicate register attempts, invalid register identifiers and paths route back to a public-safe intake path, synthetic or local controlled evidence is rejected so command-spine fixtures and local controlled smoke packets cannot count as completed office pilots, dry-run writes, and post-write status checks, `release pilot status` reports remaining office pilots, registered packet validation, per-pilot `missingEvidence`, aggregate `missingEvidenceSummary`, `evidenceCompleteOfficePilotCount`, `remainingEvidenceCompleteOfficePilotCount`, `productionSupportReviewPath`, and machine-readable `nextActions` without mutating rollout status, including status structural repair nextActions for schema, count, requiredEvidence, evidence flag, non-office evidence, and overclaim repairs, and `release pilot claim` remains dry-run by default with machine-readable `claimBlockers` and `nextActions` before an explicit completion write. Production support claims additionally require `--production-support --support-review docs/smoke/v6.0/<support-review>.md` and a recorded `productionSupportReviewPath` public-safe summary after private support review; `release pilot support-review scaffold --path docs/smoke/v6.0/<support-review>.md --output json` creates that summary from the template, emits `release-pilot-support-review-scaffold.v1`, reports `rolloutStatusMutated=false`; `release pilot support-review validate --path docs/smoke/v6.0/<support-review>.md --output json` emits `release-pilot-support-review-validate.v1`, checks the filled public-safe summary before claim, and leaves blank fields claim-blocking until private approval fills them. If a production-support claim dry-run is blocked by support review readiness, nextActions include scaffolding the summary, `complete private support review summary`, running support-review validate, and rerunning the claim dry-run. Support review creation is deferred until the completed pilot threshold is satisfied so premature production-support requests keep the next actions on pilot packet intake first. It requires doctor, status, workbench, release, ledger query/validate, ledger stats/timeline analytics snapshots, journal verify, rollback, user-review evidence, BIM manager signoff, project-copy owner signoff, support ticket review, multi-user rollout postmortem, and a packet `Pilot identifier` matching the registered pilot id while `docs/smoke/v6.0/office-rollout-status.json` records 0 completed office pilots and blocks production support claims until 2-3 office rollout pilots are completed with per-pilot evidence flags and public-safe repo-relative Markdown evidence packet paths under `docs/smoke/v6.0/`. |
| Local controlled pilot packet | local controlled evidence | `docs/smoke/v6.0/local-controlled-pilot-20260525.md` records a public-safe local packet for the controlled `revit_cli.rvt` smoke model with doctor/status, workbench/release gates, ledger query/validate/stats/timeline analytics snapshots, and journal sign/verify evidence. This packet is not office rollout completion. |
| Real Revit pilots | partial live verified | Revit 2026 add-in identity, set apply/restore, approved set ledger recording, bounded set replay apply, receipt-backed PDF export ledger recording, bounded export replay apply, manifest-backed schedule batch-export ledger recording, bounded schedule batch-export replay apply, and one local controlled pilot packet are live verified on one controlled local smoke model. Broader v6.0 live ledger apply/replay beyond approved set/export/schedule records, broader live Revit ledger integration, analytics service runtime, and office rollout pilots remain not live verified future work. |

No SaaS, no MCP, no dashboard-central workflow, no built-in LLM parser, and no
database runtime are introduced.
A local dashboard or TUI may only view receipts/reports and cannot be the
source of truth for the workbench, release gate, or operations ledger.
